TEAL'S SUNDAY PICK'EM — v0.3.0 GATE 3 INSTALL

A. SUPABASE MIGRATION
1. Keep the existing Yahtzee Supabase project and existing `pickem` exposed schema.
2. In Supabase SQL Editor, create a new query and run ALL of:
   db/003_gate3_nfl_data.sql
3. Supabase may warn that the query changes database objects. This migration adds Gate 3 columns/tables and does not drop Yahtzee or Gate 1/2 data.
4. Go to Integrations -> Data API -> Settings -> Exposed tables.
5. Enable these NEW Pick'em tables only:
   pickem.nfl_games
   pickem.nfl_players
   pickem.player_week_stats
   pickem.data_runs
   Keep the existing Pick'em tables enabled. Leave Yahtzee table settings unchanged.

B. GITHUB UPDATE
6. Upload/replace the v0.3.0 GitHub Update files in the existing teals-sunday-pickem repo.
7. requirements.txt changes, so allow Streamlit to do a full dependency rebuild.

C. GITHUB ACTIONS SECRETS — NEW FOR GATE 3
8. In GitHub repo -> Settings -> Secrets and variables -> Actions, create two Repository secrets:
   SUPABASE_URL
   SUPABASE_SECRET_KEY
   Use the SAME values already used by this Pick'em Streamlit app / Yahtzee Supabase project.
   Do NOT add PIN_PEPPER, SESSION_PEPPER, or Commissioner credentials to GitHub Actions; the NFL job does not need them.
9. The new workflow should appear under Actions as: Pickem NFL Refresh.

D. REAL PROVIDER ACCEPTANCE TEST
10. Open the deployed Pick'em app and enter Commissioner mode.
11. Click Run Gate 3 Data Check.
12. Expected before Week 1:
    - Eligible Sunday games: 13
    - QB / RB / WR / TE / K ranking counts: 10 each
    - Five player names shown as the preview at each position
    - No Week 1 early publication
13. In GitHub -> Actions -> Pickem NFL Refresh -> Run workflow, choose `diagnostic` once. This proves the scheduled runner can read the two GitHub secrets and reach the data providers/Supabase.
14. Do NOT force-publish Week 1 before Tuesday noon. The normal `auto` workflow publishes automatically once Week 1 opens.

E. TUESDAY ACCEPTANCE
15. At/after Tuesday 12:00 PM ET, confirm the automatic workflow has published the pool.
16. The normal player app should immediately show five real choices at each QB/RB/WR/TE/K instead of the prepared-pool message.
17. Verify the same Gate 2 lineup/edit/Questionable backup flow with real players.

No new Streamlit Secrets are required for Gate 3.
