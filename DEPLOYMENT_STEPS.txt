TEAL'S SUNDAY PICK'EM — v1.0.0 WEEK 1 RELEASE INSTALL

A. DATABASE / SECRETS
1. No SQL migration is required for v1.0.0.
2. Do not change Supabase exposed tables.
3. Do not change Streamlit Secrets or GitHub Actions repository secrets.

B. GITHUB UPDATE
4. Upload/replace the files from the v1.0.0 GitHub Update ZIP in the existing teals-sunday-pickem repository.
5. Make sure the workflow files have the permanent paths:
   .github/workflows/nfl-refresh.yml
   .github/workflows/quality-gate.yml
   Do not keep versioned duplicate workflow filenames.
6. Commit directly to main. Streamlit should redeploy automatically.

C. WORKFLOW CHECK
7. In GitHub → Actions, confirm Pickem Quality Gate runs green after the commit.
8. Confirm Pickem NFL Refresh remains visible. The optimized v1 schedule is intentionally sparse on weekdays and becomes every 15 minutes from the Sunday 11 AM ET window through the late-Sunday live-scoring window.

D. APP CHECK
9. Open the deployed app and confirm Build 1.0.0 appears where the build label is shown.
10. Enter Commissioner → Launch → Run Launch Check. Expect READY (or only a temporary heartbeat warning until the next successful auto worker run).
11. Run Full Week Rehearsal. Expect 9/9 PASS.

E. RELEASE
12. Commit message: v1.0.0 — Week 1 Release
13. Tag: v1.0.0
14. Keep WEEK1_LAUNCH_CHECKLIST.md available through the first real Tuesday/Sunday/Monday cycle.

No new Streamlit secrets, GitHub secrets, or Supabase changes are required for v1.0.0.
