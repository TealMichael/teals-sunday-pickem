TEAL'S SUNDAY PICK'EM — v1.0.2 INSTALL

A. DATABASE / SECRETS
1. No SQL migration is required for v1.0.2.
2. Do not change Supabase exposed tables or settings.
3. Do not change Streamlit Secrets or GitHub Actions repository secrets.

B. GITHUB UPDATE
4. Upload/replace the files from the v1.0.2 Full ZIP in the existing teals-sunday-pickem repository.
5. Keep the existing workflow paths unchanged:
   .github/workflows/nfl-refresh.yml
   .github/workflows/quality-gate.yml
6. Commit directly to main. Streamlit should redeploy automatically.

C. AUTOMATED CHECK
7. In GitHub → Actions, confirm Pickem Quality Gate runs green after the commit.
8. The expected local release checks are pytest, compileall, and release_guard.

D. APP CHECK
9. Open the deployed app and confirm Build 1.0.2 appears where the build label is shown.
10. Before Sunday lock, confirm the new Sunday Status card shows lineup readiness, injury status, lock timing, and NFL-data freshness.
11. Enter Commissioner → Launch → Run Launch Check. Expect READY (or only the known temporary heartbeat warning until the next successful auto worker run).
12. Run Full Week Rehearsal and expect 9/9 PASS.

E. MONDAY RECAP CHECK
13. The richer recap appears only when the current week data_status is FINAL after Monday reconciliation.
14. Verify the champion card, recap awards, standings, season leader, and Share Results copy area.

F. RELEASE
15. Commit message: v1.0.2 — Sunday Confidence + Weekly Recap
16. Tag: v1.0.2
17. Keep the prior v1.0.1 ZIP untouched as the rollback baseline.

No new Streamlit secrets, GitHub secrets, SQL, Supabase settings, or GitHub Actions changes are required for v1.0.2.
