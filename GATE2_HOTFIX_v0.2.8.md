# v0.2.8 — Gate 2 Native Player Card Tap Fix

- Replaces the fragile invisible-button overlay with a real full-width Streamlit button for each player card.
- The entire visible player card is now the native tap target.
- Preserves inline yellow QUESTIONABLE and red OUT status labels.
- Preserves the v0.2.6 Supabase performance pass and edit-return behavior.
- No SQL, Supabase setting, or secret changes.
