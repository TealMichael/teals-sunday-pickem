v0.2.8 — Gate 2 Native Player Card Tap Fix

Replaces the fragile player-card click overlay with a native full-card Streamlit button so the entire visible card is clickable on desktop and mobile, while preserving Gate 2 performance improvements and inline injury status.
