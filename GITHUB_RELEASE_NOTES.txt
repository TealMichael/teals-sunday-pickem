v0.5.0 — Gate 5 Commissioner Controls

Adds the production Commissioner dashboard: week status, lineup readiness, PIN resets, manual NFL refresh, failed-pool regeneration, emergency pool replacement, manual score overrides, protected final reconciliation, and consolidated diagnostics. Commissioner actions reuse the existing server-only Gate 1–4 schema; no SQL migration or new secrets are required.
